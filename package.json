{
  "name": "qbr",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@11.0.9",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --filter @qbr/client dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "measure": "pnpm --filter @qbr/sim measure",
    "sweep": "pnpm --filter @qbr/sim sweep"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
